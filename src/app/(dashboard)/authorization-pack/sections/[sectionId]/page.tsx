import { SectionWorkspaceClient } from "../../components/workspace/SectionWorkspaceClient";

export const dynamic = "force-dynamic";

export default function AuthorizationPackSectionPage() {
  return <SectionWorkspaceClient />;
}
